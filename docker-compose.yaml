version: '3'
services:
  web:
    build:
      context: ./flask
      dockerfile: Dockerfile
    ports:
      - 3001:3001
    environment:
      PORT: 3001
    depends_on:
      - db
      
  db:
    image: postgres
    volumes:
      - /home/wcap/tmp/event-maker:/var/lib/postgresql/data/
    ports:
      - 5432:5432